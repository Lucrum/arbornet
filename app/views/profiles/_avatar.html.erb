<% if user.avatar.attached? %>
  <%= image_tag user.avatar.variant(:thumb), class: "small-icon" %>
<% else %>
  <%= image_tag "icons/account-circle-outline.svg", alt: "user-icon", class: "small-icon" %>
<% end %>